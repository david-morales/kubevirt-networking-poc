apiVersion: v1
kind: ConfigMap
metadata:
  name: mongo-ansible-playbook
  namespace: {{ .Values.namespace }}
data:
  ansible_playbook.yml: |
    {{ (tpl (.Files.Glob "ansible/mongo-ansible-playbook.yml").AsConfig . ) | indent 2 }}
